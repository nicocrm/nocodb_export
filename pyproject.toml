[project]
name = "nocodb-export"
version = "0.1.0"
description = "Utility for importing and exporting data and schema from NocoDB"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
]
